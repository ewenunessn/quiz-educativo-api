# Configuração do Neon PostgreSQL
# Substitua pelos valores reais do seu projeto Neon

DB_HOST=ep-cool-math-123456.us-east-1.aws.neon.tech
DB_PORT=5432
DB_NAME=neondb
DB_USER=seu-usuario-neon
DB_PASSWORD=sua-senha-gerada-pelo-neon

# Configuração do Servidor
PORT=5000
NODE_ENV=development

# IMPORTANTE: 
# 1. Copie este arquivo para .env
# 2. Substitua os valores pelos dados reais do seu Neon
# 3. No Vercel, configure essas mesmas variáveis no painel